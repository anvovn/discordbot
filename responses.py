import discord
import responses

# Chat bot features
def get_response(message: str) -> str:
    p_message = message.lower()

    '''
    Example usage:

    if p_message == 'hello':
        return f'Hello {p_message.author}!'
    '''
    
    return None
